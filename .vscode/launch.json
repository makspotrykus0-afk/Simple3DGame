{
    "configurations": [
        {
            "name": "LLDB: Build and Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Simple3DGame.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ],
    "version": "2.0.0"
}